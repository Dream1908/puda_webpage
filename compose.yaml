name: ${COMPOSE_PROJECT_NAME:-puda-webpage}

services:
    hermes-webui:
        build:
            context: .
            dockerfile: Dockerfile.webui
        restart: unless-stopped
        env_file:
            - .env
        environment:
            HERMES_HOME: /home/hermeswebui/.hermes
            HERMES_WEBUI_AGENT_DIR: /home/hermeswebui/.hermes/hermes-agent
        volumes:
            - ${HOME:-${USERPROFILE}}/.hermes:/home/hermeswebui/.hermes
            - ${HOME:-${USERPROFILE}}/.hermes/hermes-agent:/home/hermeswebui/.hermes/hermes-agent:ro
            - ${HOME:-${USERPROFILE}}/workspace:/workspace
        ports:
            - "${HERMES_WEBUI_BIND_ADDRESS:-127.0.0.1}:${HERMES_WEBUI_PORT:-8787}:8787"
